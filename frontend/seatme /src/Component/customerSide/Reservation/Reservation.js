import React from 'react'
import DatePicker from 'react-datepicker'

// props: current date;
// props:restaurant id;
const Reservation = (props) => {    

    return(
        <div>
            <DatePicker/>
        </div>
    )
}